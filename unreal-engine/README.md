# Unreal Engine scene — coming soon

A **3D scene** for this Berlin JuniStraße sample (visualization, simulation, or digital twin workflows) is **not included yet**.

When it is ready, this directory may contain:

- Project notes, version pins (Unreal Engine / plugins), and import steps from the OpenDRIVE map and `drive-data/`
- Optional: packaged content or a link to a separate artifact store if binaries are too large for GitHub

Until then, use the OpenDRIVE asset under [`../maps/opendrive/`](../maps/opendrive/) in your own Unreal or simulator pipeline.
